/* public class AddressesService : IAddressesService
{
    private readonly IAdressesRepository _addressesRepository;
    private readonly IPropertyRepository _repository;
    public AddressesService(IAdressesRepository adressesRepository, IPropertyRepository propertyRepository)
    {
        _addressesRepository = 
    }
} */